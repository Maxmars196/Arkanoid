					//if (ball.y > myBrick.y && ball.y < (myBrick.y+myBrick.height)) {
						//if (ball.x < myBrick.x || ball.x > (myBrick.x+myBrick.width)) {
							//ball.velocityX = -ball.velocityX;
						//} else {
							//ball.velocityY = -ball.velocityY;
							//ball.velocityX = -ball.velocityX;
						//}
					//} else if (ball.x > myBrick.x && ball.x < (myBrick.x + myBrick.width)){
						//if (ball.y < myBrick.y || ball.y > (myBrick.y + myBrick.height)) {
							//ball.velocityY = -ball.velocityY;
						//} else {
							//ball.velocityY = -ball.velocityY;
							//ball.velocityX = -ball.velocityX;
						//}
					//}